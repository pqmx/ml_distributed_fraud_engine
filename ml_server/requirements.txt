# =============================================================================
# ml_server/requirements.txt
# =============================================================================
# Pinned dependencies for the Python ML server.
# Versions chosen for compatibility with grpcio-tools 1.54.x (matches the
# C++ side's gRPC 1.54.3 — keeps the protobuf wire format aligned).
# =============================================================================

fastapi==0.110.0
uvicorn[standard]==0.27.1
grpcio==1.54.3
grpcio-tools==1.54.3
protobuf==4.21.12

# Modeling stack
xgboost==2.0.3
scikit-learn==1.4.1.post1
pandas==2.2.1
numpy==1.26.4
joblib==1.3.2

# Health/debug HTTP
pydantic==2.6.4
